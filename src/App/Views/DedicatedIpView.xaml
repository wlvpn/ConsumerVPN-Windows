<UserControl x:Class="WLVPN.Views.DedicatedIpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:WLVPN.Behaviors"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:local="clr-namespace:WLVPN.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="550"
             d:DesignWidth="750"
             Background="{DynamicResource ContentBackgroundBrush}"
             mc:Ignorable="d">
    <Grid Focusable="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="20,20,30,20"
              Focusable="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="9*" MinWidth="410" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1*" MinWidth="120" />
            </Grid.ColumnDefinitions>

            <Grid Height="30"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Center"
                  Background="{DynamicResource SearchBarBackgroundBrush}"
                  Focusable="False">
                <TextBox x:Name="Search"
                         Padding="10,0,0,0"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Center"
                         VerticalContentAlignment="Center"
                         AutomationProperties.HelpText="{lex:Loc HelpTextSearch}"
                         AutomationProperties.Name="{lex:Loc Search}"
                         Background="Transparent"
                         BorderThickness="0"
                         CaretBrush="{DynamicResource IdealForegroundBrush}"
                         Focusable="True"
                         FontSize="16"
                         Foreground="{DynamicResource IdealForegroundBrush}"
                         GotKeyboardFocus="Search_GotKeyboardFocus"
                         LostKeyboardFocus="Search_LostKeyboardFocus"
                         TabIndex="1"
                         ToolTip="{lex:Loc HelpTextSearch}" />

                <StackPanel Name="searchStackPanel"
                            Margin="5,0"
                            VerticalAlignment="Center"
                            Focusable="False"
                            IsHitTestVisible="False"
                            Orientation="Horizontal">
                    <Image Width="16"
                           Height="16"
                           Source="{StaticResource MagnifyingGlassIcon}" />
                    <TextBlock Margin="5,0,0,3"
                               VerticalAlignment="Center"
                               FontSize="17"
                               IsHitTestVisible="False"
                               Style="{DynamicResource BaseTextBlock}"
                               Text="{lex:Loc Search}" />
                </StackPanel>
            </Grid>

            <Button Name="connectButton"
                    Grid.Column="2"
                    Margin="0"
                    cal:Message.Attach="[Event Click] = [Action Connect()]"
                    IsDefault="True"
                    TabIndex="2">
                <TextBlock AutomationProperties.HelpText="{lex:Loc HelpTextConnectButton}"
                           FontSize="15"
                           Style="{DynamicResource BaseTextBlock}"
                           Text="{lex:Loc Connect}"
                           ToolTip="{lex:Loc HelpTextConnectButton}" />
            </Button>
        </Grid>

        <!--  the major portion of the data grid style is defined in Resources/NamedStyles/MetroDataGrid.xaml  -->
        <!--
            ponytail: expanded state lives on the row container, so recycling would leak it between rows,
            hence EnableRowVirtualization="False". An account holds a handful of dedicated IPs; move to a
            per-item IsExpanded flag if these lists ever grow.
        -->
        <DataGrid x:Name="DedicatedIpDataGrid"
                  Grid.Row="1"
                  Margin="20,0,0,0"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  behaviors:LocationSortBehaviour.AllowCustomSort="True"
                  AutoGenerateColumns="False"
                  Background="Transparent"
                  BorderBrush="Transparent"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="True"
                  EnableRowVirtualization="False"
                  GridLinesVisibility="None"
                  HorizontalScrollBarVisibility="Disabled"
                  IsReadOnly="True"
                  ItemsSource="{Binding Locations}"
                  IsTabStop="False"
                  KeyboardNavigation.TabNavigation="Continue"
                  PreviewKeyDown="DedicatedIpDataGrid_PreviewKeyDown"
                  RowDetailsVisibilityMode="Collapsed"
                  RowHeaderWidth="0"
                  ScrollViewer.CanContentScroll="True"
                  SelectedItem="{Binding Path=SelectedLocationItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  Style="{StaticResource MetroDataGrid}"
                  TabIndex="3">
            <DataGrid.Resources>
                <Style x:Key="DedicatedIpColumnHeader"
                       BasedOn="{StaticResource MetroDataGridColumnHeader}"
                       TargetType="DataGridColumnHeader">
                    <Setter Property="IsTabStop" Value="False" />
                </Style>

                <behaviors:CountrySorter x:Key="CountrySorter" />
                <behaviors:CitySorter x:Key="CitySorter" />
                <behaviors:LoadSorter x:Key="LoadSorter" />

                <Style x:Key="DedicatedIpHeader" TargetType="TextBlock">
                    <Setter Property="Margin" Value="10,6" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Foreground" Value="{DynamicResource IdealForegroundDisabledBrush}" />
                </Style>

                <Style x:Key="DedicatedIpCell" TargetType="TextBlock">
                    <Setter Property="Margin" Value="10,4" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Foreground" Value="{DynamicResource IdealForegroundBrush}" />
                </Style>

                <!--  a dedicated server row, selectable and double clickable in its own right  -->
                <Style x:Key="DedicatedIpServerRow" TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="cal:Message.Attach" Value="[Event MouseDoubleClick ] = [ConnectFromServer($dataContext, $eventArgs)]" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Stretch" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <!--  matches MetroDataGridRow, where focusing a row selects it  -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter Property="IsSelected" Value="True" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MOUSEOVER_LOCATION_SERVERS_LIST_BACKGROUND}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource SELECTED_LOCATION_SERVERS_LIST_BACKGROUND}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>

                <!--
                    The row's DetailsVisibility is the only expanded/collapsed state, so the glyph stays
                    right whether the row was expanded by mouse or by the Left/Right arrow keys.
                -->
                <Style x:Key="DedicatedIpExpander" TargetType="Button">
                    <Setter Property="Width" Value="16" />
                    <Setter Property="Height" Value="16" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="Transparent">
                                    <Path x:Name="Arrow"
                                          Width="7"
                                          Height="10"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 0,0 L 6,5 L 0,10 Z"
                                          Fill="{DynamicResource IdealForegroundBrush}"
                                          RenderTransformOrigin="0.5,0.5"
                                          Stretch="Uniform" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DetailsVisibility, RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}}}" Value="Visible">
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="90" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.ColumnHeaderStyle>
                <StaticResource ResourceKey="DedicatedIpColumnHeader" />
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.RowStyle>
                <Style BasedOn="{StaticResource MetroDataGridRow}" TargetType="DataGridRow">
                    <Setter Property="cal:Message.Attach" Value="[Event MouseDoubleClick ] = [ConnectFromLocation($dataContext, $source, $eventArgs)]" />
                </Style>
            </DataGrid.RowStyle>

            <!--  expanded row: the dedicated IP servers held in this city  -->
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <!--  opaque border, otherwise the selected row's highlight bleeds into the details  -->
                    <Border Background="{DynamicResource ContentBackgroundBrush}">
                        <Grid Margin="0,5,0,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Background="{DynamicResource TabHeaderBackgroundBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="110" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Style="{StaticResource DedicatedIpHeader}"
                                           Text="{lex:Loc Server}" />
                                <TextBlock Grid.Column="1"
                                           Style="{StaticResource DedicatedIpHeader}"
                                           Text="{lex:Loc DedicatedIp}" />
                                <TextBlock Grid.Column="2"
                                           Margin="5,6"
                                           Style="{StaticResource DedicatedIpHeader}"
                                           Text="{lex:Loc Load}" />
                            </Grid>

                            <ListBox Grid.Row="1"
                                     Padding="0"
                                     HorizontalContentAlignment="Stretch"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ItemContainerStyle="{StaticResource DedicatedIpServerRow}"
                                     IsTabStop="False"
                                     ItemsSource="{Binding Children}"
                                     KeyboardNavigation.TabNavigation="Continue"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                     SelectedItem="{Binding DataContext.SelectedServer, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                     SelectionMode="Single">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid MinHeight="30" Background="Transparent">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="110" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0"
                                                       Style="{StaticResource DedicatedIpCell}"
                                                       Text="{Binding Hostname}" />
                                            <TextBlock Grid.Column="1"
                                                       Style="{StaticResource DedicatedIpCell}"
                                                       Foreground="{DynamicResource AccentBrush}"
                                                       Text="{Binding DedicatedIpAddress, Mode=OneWay}" />
                                            <TextBlock Grid.Column="2"
                                                       Margin="5,4"
                                                       Style="{StaticResource DedicatedIpCell}"
                                                       Text="{Binding Load, StringFormat='{}{0}%'}" />
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>

            <DataGrid.Columns>
                <DataGridTemplateColumn Width="30"
                                        CanUserResize="False"
                                        IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    AutomationProperties.Name="{lex:Loc ExpandCollapse}"
                                    Click="Expander_Click"
                                    Style="{StaticResource DedicatedIpExpander}"
                                    ToolTip="{lex:Loc ExpandCollapse}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="160"
                                        CanUserResize="False"
                                        IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Width="32"
                                   Height="32"
                                   Margin="7"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   Source="{Binding CountryCode, Converter={StaticResource CountryCodeToFlagConverter}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="*"
                                        behaviors:LocationSortBehaviour.CustomSorter="{StaticResource CountrySorter}"
                                        Header="{lex:BLoc Country}"
                                        IsReadOnly="True"
                                        SortMemberPath=".">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       FontSize="15"
                                       Style="{DynamicResource BaseTextBlock}"
                                       Text="{Binding Country}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="*"
                                        behaviors:LocationSortBehaviour.CustomSorter="{StaticResource CitySorter}"
                                        Header="{lex:BLoc City}"
                                        IsReadOnly="True"
                                        SortMemberPath=".">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       FontSize="15"
                                       Style="{DynamicResource BaseTextBlock}"
                                       Text="{Binding City}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="110"
                                        behaviors:LocationSortBehaviour.CustomSorter="{StaticResource LoadSorter}"
                                        Header="{lex:BLoc Load}"
                                        IsReadOnly="True"
                                        SortMemberPath=".">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Margin="5,0"
                                       VerticalAlignment="Center"
                                       FontSize="15"
                                       Style="{DynamicResource BaseTextBlock}"
                                       Text="{Binding Load, StringFormat='{}{0}%'}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
