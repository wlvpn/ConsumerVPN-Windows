<UserControl x:Class="WLVPN.Views.Information.LicenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:local="clr-namespace:WLVPN.Views.Information"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:properties="clr-namespace:WLVPN.Properties"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="{StaticResource TabContentBackgroundBrush}"
             mc:Ignorable="d">

    <ScrollViewer Padding="53,20,0,0"
                  HorizontalAlignment="Stretch"
                  Background="Transparent"
                  CanContentScroll="True"
                  DockPanel.Dock="Bottom"
                  HorizontalScrollBarVisibility="Hidden"
                  PanningMode="Both">
        <RichTextBox x:Name="LicenseRichBox"
                     Background="Transparent"
                     BorderThickness="0"
                     IsReadOnly="True">
            <FlowDocument Name="LicenseRichBoxFlowDocument"
                          Foreground="{DynamicResource IdealForegroundBrush}"
                          PageWidth="{Binding ElementName=LicenseRichBox, Path=ActualWidth}" />
        </RichTextBox>
    </ScrollViewer>

</UserControl>
