<UserControl x:Class="WLVPN.Views.Login.InProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:MahApps.Metro.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:extensions="clr-namespace:WLVPN.Extensions"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <controls:ProgressRing Width="55" Height="55" />
        <TextBlock Margin="0,20,0,0"
                   HorizontalAlignment="Center"
                   FontSize="16"
                   Foreground="{DynamicResource AccentBrush}"
                   Style="{DynamicResource BaseTextBlock}"
                   Text="{extensions:LocProgress LoggingIn}" />
    </StackPanel>
</UserControl>